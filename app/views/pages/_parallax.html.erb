<% @pagescontents.each do |pagescontent| %>
<div class="parallax-container valign-wrapper" id="parallax-container">
	<div class="section no-pad-bot">
		<div class="container">
			<div class="row center">
				<h5 class="header col s12 light"><%= pagescontent.name %></h5>
			</div>
		</div>
	</div>
	<div class="parallax">
	</div>
</div>
<% end %>