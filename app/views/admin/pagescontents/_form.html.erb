<%= form_for([:admin, @pagescontent]) do |f| %>
  <% if @pagescontent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pagescontent.errors.count, "error") %> prohibited this pagescontent from being saved:</h2>

      <ul>
      <% @pagescontent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>