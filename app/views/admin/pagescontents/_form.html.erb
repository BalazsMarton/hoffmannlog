<div class="col s12 m12 l12">

<%= form_for([:admin, @pagescontents]) do |f| %>
  <% if @pagescontents.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pagescontents.errors.count, "error") %> prohibited this pagescontent from being saved:</h2>

      <ul>
      <% @pagescontents.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
  <div class="file-field input-field col s12 m4 l4">
    <div class="btn">
      <span>file</span>
      <%= f.file_field :image %>
      </div>
    <input class="file-path validate" type="text"/>
  </div>
  </div>

  <div class="row">
  <div class="input-field col s12 m4 l4">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  </div>

  <div class="row">
  <div class="input-field col s12 m12 l12">
    <%= f.text_area :content, :class => 'ckeditor'%>
  </div>
  </div>

  <div class="row">
  <div class="actions col s12 m12 l12 center">
    <%= f.submit class: 'waves-effect waves-light btn' %>
  </div>
  </div>

<% end %>

</div>