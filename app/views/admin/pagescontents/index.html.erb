<h1>Page Contents</h1>

<table>
  <thead>
    <tr>
    	<th>Image</th>
      	<th>Page</th>
      	<th>Content</th>
      	<th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% @pagescontents.each do |pagescontent| %>
      <tr>
        <td><%= pagescontent.image %></td>
        <td><%= pagescontent.name %></td>
        <td><%= pagescontent.content %></td>
        <td><%= link_to 'Show', [:admin, pagescontent] %></td>
        <td><%= link_to 'Edit', edit_admin_pagescontent_path(pagescontent) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
